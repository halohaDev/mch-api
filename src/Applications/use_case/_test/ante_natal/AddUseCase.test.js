describe('AddAnteNatalUseCase', () => {
  it('should orchestrating the add ante natal action correctly when no previous maternal_histories', async () => {
    // Arrange

    // mocking needed function

    // creating use case instance

    // Action

    // Assert
  });

  it('should orchestrating the add ante natal action correctly when its has active  maternal_histories', async () => {
    // Arrange

    // mocking needed function

    // creating use case instance

    // Action

    // Assert
  });

  it('should orchestrating the add ante natal action correctly when its has non active maternal_histories', async () => {
  });
});
